// some tasks are arriving at diff time having diff execution time, pc select that task which has least execution time
// and once it starts a task, it completes it 
// if two task have same execuion time, which comes first is executed first
// if both these things are same, then one having less id will be done first
// everytime has a unique id associated with it 
// Q find the order in which tasks will be processed